# AI Skills — ทะเบียนกฎหมาย SHE

ระบบมี AI Skill 2 ตัว ทำงานเป็น Vercel serverless function เรียกจากหน้า **สรุปกฎหมาย** (`src/pages/LawSummary.jsx`)
ต้องตั้ง env `ANTHROPIC_API_KEY` (และเลือกได้ `ANTHROPIC_MODEL` — ค่าเริ่มต้น `claude-sonnet-4-6`)

---

## โฟลว์ 2 คำขอ (P16)

การสรุป 1 ครั้งยิง **2 คำขอเรียงกัน** ไม่ใช่คำขอเดียว

```
คำขอ 1  POST /api/law-analyze  { relate: false }   → ตารางฉบับหลัก ขึ้นทันที
คำขอ 2  POST /api/law-relate   { refs: pending_refs }  → เติมข้อจากกฎหมายที่อ้างถึง
```

**ทำไมต้องแยก:** เดิมทำ 4 ด่านต่อกันในคำขอเดียว (สรุปฉบับหลัก → Skill 3 ชั้น 1 → ชั้น 2 → เรียบเรียงข้อ)
รวมแล้วชนเพดาน `maxDuration` 300 วิของ Vercel เป็นประจำ โค้ดจึงต้องข้ามงานทิ้งเมื่อเวลาไม่พอ
ผลคือ **ได้ข้อมูลไม่ครบ ไม่ใช่แค่ช้า** · แยกแล้วแต่ละฝั่งได้ 300 วิของตัวเอง และผู้ใช้เห็นตารางก่อน

คำขอที่ 2 ล้มเหลว **ไม่ล้างผลของคำขอแรก** — หน้าเว็บขึ้นปุ่ม "ลองเติมกฎหมายที่อ้างถึงอีกครั้ง"
ยิงซ้ำเฉพาะด่านนั้น ซึ่งเร็วขึ้นมากเพราะ cache

**ผลลงจอครั้งเดียวตอนจบ** (แก้ 2026-08-17) — เดิมด่านแรกจบแล้วตารางขึ้นทันที แล้วข้อจาก
กฎหมายที่อ้างถึงค่อยเติมเข้ามาทีหลัง · ผู้ใช้อ่านสรุปที่ยังไม่ครบแล้วแยกไม่ออกว่าที่เห็นคือของจริง
หรือของชั่วคราว จึงเปลี่ยนเป็นถือผลของด่านแรกไว้ในตัวแปร แล้ว `commit()` พร้อมกันทีเดียวเมื่อด่านสองเสร็จ
**ยังยิง 2 คำขอเหมือนเดิม** — รวมเป็นคำขอเดียวจะกลับไปชนเพดาน 300 วิ ซึ่งเป็นเหตุผลที่แยกไว้ตั้งแต่แรก
ที่เปลี่ยนคือ *จังหวะแสดงผล* ไม่ใช่สถาปัตยกรรม · ด่านสองล้มเหลวยังลงผลของด่านแรกให้เหมือนเดิม
ระหว่างรอขึ้นข้อความบอกขั้น ("ขั้นที่ 1 จาก 2" / "ขั้นที่ 2 จาก 2 · กำลังตามอ่าน N ฉบับ")

---

## Skill 1+2 — อ่าน / วิเคราะห์ / สรุปกฎหมาย
ไฟล์: `api/law-analyze.js`

**Input**
```
POST /api/law-analyze
{ "source": "<URL หรือ ตัวบท>", "kind": "auto|text",
  "pdfBase64": "<ไฟล์ PDF>", "sourceUrl": "", "stage": false, "relate": false }
```
รับได้ 3 ทาง: **ลิงก์ไฟล์ .pdf** (Claude ดึงเอง) · **แนบไฟล์ PDF** (≤4.5MB) · **ลิงก์หน้าเว็บ/วางข้อความ**
`relate: false` = คืนผลฉบับหลักอย่างเดียว (ค่าเริ่มต้นคือ `true` เพื่อความเข้ากันได้กับของเดิม)

**สิ่งที่สกัดออกมา**
- ระดับกฎหมาย: ชื่อเต็ม · ประเภท · กระทรวง · วันที่ประกาศ · **กฎวันบังคับใช้** · เอกสารที่ต้องจัดทำ ·
  หมวด (LA–LG) · เลขอ้างอิงราชกิจจาฯ (`gazette_ref`) · **บทยกเลิก** (`repeals`)
- ระดับข้อกำหนด: เลขมาตรา/ข้อ · เนื้อหา · `source_excerpt` · ผู้รับผิดชอบ · ขอบเขต · วิธีปฏิบัติ · เอกสาร · ความถี่ · โทษ
- กฎหมายที่ถูกอ้างถึง (`related_laws`) พร้อม `appears_in` และ `needs_lookup`

**การตรวจสอบฝั่งโค้ด (ไม่เชื่อ AI ตรงๆ)**
| ด่าน | ไฟล์ | ทำอะไร |
|---|---|---|
| วันบังคับใช้ | `_lib/effective-date.js` | AI บอกแค่ "ใช้กฎข้อไหน กี่วัน" **โค้ดบวกวันเอง** · กฎที่อ้างว่าตัวบทเขียนไว้ต้องมี `excerpt` ยืนยัน · ตัวบทไม่ระบุ = `not_stated` ใช้วันประกาศเป็นค่าตั้งต้นพร้อมป้ายเตือน |
| ตัวเลขแต่งเติม | `_lib/verify-numbers.js` | อ่านตัวเลขไทยทั้ง 3 แบบ (อารบิก · เลขไทย · คำไทย) แล้วเทียบว่าตัวเลขใน `req_text` มีที่มาใน `source_excerpt` จริง · ไม่ตรง = ติดป้ายเตือน ไม่ลบ |
| อ้างถึงจริงหรือเปล่า | `law-analyze.js` (`verifyRelatedRefs`) | `appears_in` ต้องคัดข้อความคำต่อคำ แล้วเทียบกับตัวบทจริง · ไม่ผ่าน = ไม่ตามไปดึง (กันเสียเงินฟรี) |
| แตกข้อความจาก PDF | `pdf-parse` (lazy import) | ใช้เป็นหลักฐานให้ 2 ด่านบน · ราชกิจจาฯ บางฉบับฝังฟอนต์ที่ถอดรหัสไม่ออก จึงมีด่านวัดคุณภาพก่อนใช้ (อักขระไทย ≥60% + เจอคำที่ตัวบทกฎหมายต้องมี ≥2 คำ) ไม่ผ่าน = ถอยไปโหมดตรวจอ่อน |

**บทเฉพาะกาลไม่ใช่เหตุให้ข้ามโดยอัตโนมัติ** (แก้ 2026-08-17)
เกณฑ์การข้ามคือ *"ข้อนั้นสั่งใครให้ทำอะไรหรือเปล่า"* ไม่ใช่ *"อยู่ใต้หัวข้ออะไร"* — เกณฑ์เดียวกับด่านอื่นทั้งระบบ
คือตัดสินจากเนื้อหา ไม่ใช่จากป้าย · บทเฉพาะกาลที่บอกเจ้าหน้าที่รัฐว่าระหว่างรอประกาศให้ใช้เกณฑ์ใดไปพลางก่อน
ข้ามได้ (ย้ายไปเป็น `interim_rule` ของ ref ที่เป็น `pending`) แต่ข้อที่สั่งผู้อยู่ใต้บังคับกฎหมายให้ลงมือทำ
โดยเฉพาะที่มีกำหนดเวลา **ห้ามข้าม**
> เจอจริง: **ข้อ 24** กฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560 — *"กิจการที่ตั้งขึ้นก่อนวันที่กฎกระทรวงนี้
> ใช้บังคับ ต้องปรับปรุงแก้ไข…ภายในหนึ่งปี"* อยู่ใต้หัวข้อ "บทเฉพาะกาล" ร่วมกับข้อ 23 รุ่นก่อนจึงข้ามทิ้งทั้งคู่
> ทั้งที่ข้อ 24 เป็นหน้าที่เต็มตัวพร้อมเส้นตาย (ครบกำหนด 04/08/2561) ที่ผู้ตรวจ ISO ขอดูหลักฐานได้

**Output** — `{ law, requirements, repeals, pending_refs, rejected_count, effective_source, effective_note, ... }`
`stage: true` (ค่าเริ่มต้น) จะเขียนลง `lg_import_staging` ด้วย · หน้าเว็บใช้ `stage: false` แล้วเก็บเองผ่าน `lg_ai_discovered_laws`

---

## Skill 3 — ตามอ่านกฎหมายที่ตัวบทอ้างถึง
ไฟล์: `api/_lib/osh-law-relate.js` · `_lib/anchor-answer.js` · `_lib/ref-classify.js` · endpoint `api/law-relate.js`

**ปัญหาที่แก้:** กฎกระทรวงมักไม่เขียนซ้ำสิ่งที่อยู่ใน พ.ร.บ. แม่ · จป. ที่อ่านเฉพาะกฎกระทรวงจึงตกข้อกำหนดโดยไม่รู้ตัว

---

### P17 · จำแนกการอ้างถึง 3 ชนิด ก่อนตัดสินใจว่าจะทำอะไร

ตัวบทไทยส่วนใหญ่ **ไม่ได้อ้างมาตราเจาะจง** แต่อ้างลอย ๆ ว่า "ตามกฎหมายว่าด้วยการควบคุมอาคาร"
ซึ่งเป็นกฎหมายทั้งชุดหลายร้อยหน้า · รุ่นก่อนไปดึงทั้งฉบับแล้วเลือกมา 15 ข้อแรก
ได้ข้อกำหนดที่ไม่เกี่ยวกับสิ่งที่ข้อนั้นถามถึงเลย = ผู้ใช้รู้สึกว่าระบบมั่ว

โค้ดจำแนกเองจาก `appears_in` (ข้อความคำต่อคำจากตัวบท) **ไม่เชื่อป้ายที่โมเดลติดมา**
ป้ายของโมเดลเก็บไว้ใน `ai_ref_type` เพื่อเทียบเฉย ๆ

| `ref_type` | ตัวอย่าง | การจัดการ | ค่าใช้จ่าย |
|---|---|---|---|
| `specific` | "ตามความในมาตรา 31" | ดึงมาตรานั้น (โฟลว์เดิม) | 1–2 คำขอ |
| `whole_law` | "ตามกฎหมายว่าด้วยการควบคุมอาคาร" | **ห้ามดึงทั้งฉบับ** → แปลงเป็นคำถามก่อน | 1–3 คำขอ |
| `pending` | "ตามที่รัฐมนตรีประกาศกำหนด" | **recheck ว่าประกาศออกแล้วหรือยัง** ก่อนพูดอะไรเกี่ยวกับมัน | 0–1 คำขอ |

`pending` สำคัญที่สุด — ในกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560 มีถึง **7 จุดจาก 20 ข้อ**
รุ่นก่อนไปค้นแล้วไม่เจอ จึงคืน `not_found` เหมือนกรณีค้นพลาด ซึ่งอ่านแล้วเข้าใจผิดว่าระบบพลาด
หน้าเพิ่มกฎหมายจึงตั้ง "รอผู้เกี่ยวข้องประเมิน" ให้ล่วงหน้า ไม่ปล่อยให้กลายเป็น NC ปลอม

> ### ⚠ ห้ามเขียนว่า "ยังไม่มีตัวบท" — เราไม่เคยค้น (แก้ 2026-08-17)
> รุ่นก่อน `pending` = **0 คำขอ** แปลว่าระบบ *ไม่ได้ตรวจ* ว่าประกาศฉบับนั้นออกแล้วหรือยัง
> ข้อความที่คืนกลับ ("รอ…ออกประกาศ ยังไม่มีตัวบทให้ปฏิบัติตาม") จึงเป็นการยืนยันเชิงลบจากการไม่ได้ตรวจ
>
> **ไล่เปิดประกาศของกรมอนามัยทุกฉบับตั้งแต่ปี 2560 (43 ฉบับ) แล้วเกรปหาบทอาศัยอำนาจ พบว่า 4 ใน 7 จุดออกแล้ว:**
>
> | ข้อที่เคยติดป้าย pending | ประกาศที่ออกมาแล้วจริง |
> |---|---|
> | ข้อ 3 วรรคหนึ่ง | ค่ามาตรฐานมลพิษทางเสียงฯ **พ.ศ. 2561** — เสียงรบกวนไม่เกิน 10 dBA |
> | ข้อ 4 | มาตรการควบคุมกำกับดูแลกิจการให้บริการดูแลผู้สูงอายุที่บ้านฯ **พ.ศ. 2564** · หลักเกณฑ์ด้านสุขลักษณะและมาตรการป้องกันโควิด 19ฯ **พ.ศ. 2566** |
> | ข้อ 11 วรรคสาม | กำหนดสถานประกอบกิจการที่ต้องจัดให้มีบ่อดักไขมัน **พ.ศ. 2565** |
> | ข้อ 12 | หลักเกณฑ์การป้องกันและกำจัดแมลงและสัตว์ที่เป็นพาหะนำโรคฯ **พ.ศ. 2564** |
>
> กฎกระทรวงออกปี 2560 แต่ประกาศทยอยออกตามมาอีก 1–6 ปี — **ยิ่งกฎหมายแม่เก่า ยิ่งมีโอกาสว่าออกแล้ว**
>
> **สิ่งที่แก้:**
> 1. `recheckPending()` (`_lib/anchor-answer.js`) — ตั้งคำถามว่า *"มีประกาศที่ออกตามความในข้อ X แห่ง&lt;กฎหมายแม่&gt; ออกมาแล้วหรือยัง"*
>    แล้วส่งเข้าโฟลว์เดียวกับ `whole_law` ทุกประการ ด่านตรวจบังคับครบเหมือนเดิม
>    ตอบได้ = ออกแล้วจริงและได้เนื้อหามาด้วย · ตอบไม่ได้ = **ยังไม่ยืนยัน ไม่ใช่ "ไม่มี"**
> 2. เพดาน `MAX_PENDING_RECHECK` = 6 · ใช้งบที่เหลือหลัง `whole_law` เอาไปแล้ว
>    ตรวจไม่ทัน/งบหมด = คืนข้อความที่ไม่ยืนยันอะไรทั้งนั้น ซึ่งยังปลอดภัยกว่าเดาว่าไม่มี
> 3. `pendingAnswer()` เขียนว่า *"ระบบยังไม่ได้ตรวจว่าประกาศออกมาแล้วหรือยัง ต้องเปิดเว็บของหน่วยงานตรวจก่อน"*
>    · `confidence` เป็น `low` ไม่ใช่ `high` · ป้ายใน UI เป็น **"ต้องตรวจสอบเพิ่มเติม"** สีเตือน
> 4. ย้ายออกจากกลุ่ม "เสร็จแล้ว" ในแผงกฎหมายที่อ้างถึง = **นับเป็นงานค้าง**
>    อย่างมากคือ จป. เปิดดูแล้วพบว่ายังไม่ออก เสียเวลาไม่กี่นาที ตรงข้ามกับการข้ามไป
>    ซึ่งทำให้ตกข้อที่มีหน้าที่ต้องทำจริงโดยไม่มีใครรู้

เก็บเพิ่ม 2 ฟิลด์: `issuing_authority` (รอใคร) · `interim_rule` (ระหว่างรอให้ทำอย่างไร)
`interim_rule` ต้องมีข้อความจากตัวบทรองรับ ไม่งั้นทิ้ง — ปล่อยผ่านเท่ากับให้โมเดลแต่งวิธีปฏิบัติเอง

### P17 · anchored question — แปลงการอ้างถึงเป็นคำถามก่อนค้น

`whole_law` **ห้ามส่งชื่อกฎหมายไปค้นตรง ๆ** · ให้ AI สร้าง `anchor_question` จากบริบทของข้อ
คำถามที่ใช้ได้ต้องครบ 3 อย่าง: **เรื่อง** + **บริบทของผู้ถาม** + **สิ่งที่ต้องการเป็นคำตอบ**

> ข้อ 9 "ต้องมีห้องน้ำและห้องส้วม ตามแบบและจำนวนที่กำหนดในกฎหมายว่าด้วยการควบคุมอาคาร"
> → "อาคารสำนักงานหรือสถานประกอบกิจการ ต้องมีที่ถ่ายอุจจาระ ที่ถ่ายปัสสาวะ และอ่างล้างมือ
>    อย่างละกี่ที่ ต่อพื้นที่อาคารหรือจำนวนคนเท่าไร กฎกระทรวงฉบับใด ข้อใด"

คำถามกว้างอย่าง "กฎหมายควบคุมอาคารกำหนดอะไรบ้าง" ถูก `questionUsable()` ปฏิเสธ **ก่อนยิง API**
ค้นแล้วบังคับให้ตอบคำถามนั้นโดยตรง · ตอบไม่ได้ = `not_answered` พร้อมบอกว่าเปิดไปเจออะไร
**ห้ามคืนข้อกำหนดอื่นที่ไม่เกี่ยวมาแทน** — นี่คือสาเหตุหลักที่ผลลัพธ์เดิมดูมั่ว

### P17 · ตามต่อไปยังตาราง/บัญชีท้ายกฎหมาย

กฎหมายไทยมักเขียนเกณฑ์ตัวเลขไว้ใน **ตารางท้ายกฎกระทรวง** ไม่ใช่ในเนื้อความของข้อ
คำตอบจริงของเคสข้างบนอยู่ใน *ตารางที่ 2 ท้ายกฎกระทรวง ฉบับที่ 39 (พ.ศ. 2537)*
ไม่ได้อยู่ในข้อ 8 ที่อ้างถึงตารางนั้น — ระบบที่ดึงเฉพาะข้อความของมาตราจะได้ว่า
"ให้เป็นไปตามตารางที่ 2" ซึ่ง**ไม่ใช่คำตอบ**

เจอคำว่า "ตามตารางที่" · "ท้ายกฎกระทรวงนี้" · "บัญชีท้าย" · "ตามภาคผนวก" · "แนบท้าย"
→ ถือว่ายังไม่ได้คำตอบ ส่งไฟล์ PDF ฉบับเต็มให้อ่านอีกรอบ ถอดเฉพาะแถวที่ตรงคำถาม พร้อมหัวตาราง
→ ตั้ง `from_table = true` · ถอดไม่สำเร็จ = `not_answered` **แต่ยังบอกที่อยู่ของคำตอบ + ลิงก์**

**รอบถอดตารางส่งเฉพาะหน้าที่มีตาราง ไม่ใช่ไฟล์ทั้งฉบับ** (`pdfPagesAround` · แก้ 2026-08-17)
วัดกับกฎกระทรวงควบคุมอาคาร ฉบับที่ 39: ไฟล์เต็ม ~27,500 token → ตัดหน้า ~3,500 token (**ลด 82%**)
โดยเนื้อหาที่ใช้ตอบครบเท่าเดิม (แถวสำนักงาน · แถวโรงงาน · หัวตาราง · เชิงอรรถว่าถูกฉบับ 63 แทนที่)
- เลือกหน้าที่พบชื่อตาราง **ครั้งสุดท้าย** ไม่ใช่ครั้งแรก — ชื่อตารางโผล่ 2 ที่เสมอ คือในเนื้อความที่
  อ้างถึงมัน กับที่ตัวตารางเองซึ่งอยู่ท้ายเล่ม · จับครั้งแรกจะได้หน้าที่ไม่มีตัวเลขสักตัว
- เทียบข้อความแบบ **ตัดวรรณยุกต์ทิ้ง** เพราะฉบับรวมของ ASA ทำวรรณยุกต์หายบางตัว
  ("ตารางที่ 2" ในคำใบ้ ต้องเทียบติดกับ "ตารางที  2" ในไฟล์)
- ตัดไม่ได้ (หาหน้าไม่เจอ · อักขระไทย < 50%) → ถอยไปส่งไฟล์ทั้งฉบับตามเดิม ไม่ใช่ล้มเหลว

**สูงสุด 3 รอบต่อ 1 คำถาม** — ค้นด้วยคำถาม → อ่านไฟล์ตัวบทจริง → ถอดตาราง
**แต่ปกติใช้รอบเดียว**: ข้ามรอบอ่านไฟล์เมื่อผลค้นผ่านครบทุกข้อ (ตอบได้ · โดเมนเชื่อถือได้ ·
มี `source_excerpt` ≥ 20 ตัวอักษร · **ไม่ได้ชี้ต่อไปตาราง**) ข้อสุดท้ายสำคัญที่สุด —
ชี้ไปตารางแปลว่ายังไม่ได้คำตอบ ต้องอ่านไฟล์ต่ออยู่ดี

เพดาน `MAX_ANCHOR_LOOKUPS` = `MAX_TOTAL_LOOKUPS` (12) — **ตั้งเท่ากันเพื่อให้ตามต่อได้ครบทุกจุด**
เดิมตั้ง 6 แล้วกฎกระทรวงฉบับเดียวมีจุดที่ต้องถาม 8 จุด → ตกไป 2 จุด ผู้ใช้ยังต้องเปิดเอง
ที่ตั้งสูงได้โดยไม่ชนเพดานเวลา: คำถามยิงขนานกัน (เวลารวม ≈ สายที่ยาวที่สุด ไม่ใช่ผลรวม) ·
**`whole_law` กับ `pending recheck` อยู่ในคลื่นเดียวกัน** — เคยแยกเป็นสอง `await Promise.all`
เรียงกัน เวลารวมจึงเป็นผลบวกทั้งที่ไม่มีอะไรผูกกัน (regression ตอนเพิ่ม recheck 2026-08-17) ·
`pending` ไม่เสียคำขอเลย · และรอบอ่านไฟล์ถูกข้ามในกรณีปกติ

**`pending` ไม่ผ่านด่าน `needs_lookup`** — มันไม่ใช่การ "ไปดึงอะไร" และไม่เสียเงิน
กรองทิ้งตามที่โมเดลตั้งมาจะเสียสถานะ "รอประกาศ" ไปฟรี ๆ ทั้งที่นั่นคือข้อมูลที่เปลี่ยนวิธีประเมิน

---

**ด่านตรวจ — ห้ามผ่อนทุกด่าน**
1. **(ก) โดเมน** — `source_url` ต้องอยู่ใน `TRUSTED_DOMAINS` (`_lib/law-source.js` ชุดเดียวทั้งระบบ)
2. **(ข) หลักฐาน** — ทุก requirement ต้องมี `source_excerpt` · คำตอบก็ต้องมี · ไม่มี = ไม่นับว่าตอบได้
3. **(ค) ภาษา** — `answer_plain` ที่มีคำว่า "ตามที่กฎหมายกำหนด" = ยังไม่ได้ตอบ → `not_answered`
4. **(ง) ตัวเลข** — `flagUnverifiedNumbers` ตรวจคำตอบด้วย ไม่ใช่แค่ข้อปฏิบัติ
5. **(จ) เพดานข้อ** — สูงสุด `MAX_REQ_PER_LAW` (15) ข้อต่อกฎหมาย 1 ฉบับ
6. **(ฉ) สรุปสถานะจากหลักฐาน** — ไม่ใช่จากป้ายที่โมเดลติดมา
   ข้อกำหนด > คำอธิบายที่ยืนยันได้ > ลิงก์อย่างเดียว (`link_only`) > ไม่มีอะไรเลย

**ความลึก (เฉพาะ `specific`)** — ชั้น 2 ตามต่อ *เฉพาะการมอบอำนาจ* คือฉบับชั้น 1 อ่านแล้วยังชี้ต่อ
เพดานรวม `MAX_TOTAL_LOOKUPS` = 12 ทุกชั้นรวมกัน · ชั้น 2 สูงสุด 4

**โดเมนที่เชื่อถือได้ — 2 ชั้น** (`_lib/law-source.js` · ชุดเดียวทั้งระบบ)
- **ชั้น 1 ต้นทาง** — หน่วยงานที่ออกกฎหมายนั้นเอง (ราชกิจจาฯ · dlpw · moph · diw · dpt · rd · nbtc …)
- **ชั้น 2 ฉบับรวมขององค์กรวิชาชีพ** (asa.or.th · shawpat.or.th) — **แม่นกว่าสำหรับกฎหมายที่ถูกแก้หลายรอบ**
  เพราะรวมฉบับแก้ไขไว้พร้อมเชิงอรรถ · เคสจริง: ตารางที่ 2 ของกฎกระทรวง ฉบับที่ 39 (พ.ศ. 2537)
  ถูกยกเลิกโดยฉบับที่ 63 (พ.ศ. 2551) — ดึงราชกิจจาฯ ฉบับ 39 ตรง ๆ จะได้ตารางที่ถูกยกเลิกแล้ว
  **โดยไม่มีอะไรเตือน** ซึ่งอันตรายกว่า · คำตอบจากชั้น 2 ติดป้าย "ตรวจกับต้นฉบับก่อนใช้อ้างอิง" ทุกครั้ง
- `krisdika.go.th` **ห้ามใส่กลับ** — apex ไม่มี DNS record · `www.` ใช้ SSL self-signed → `fetch()` ปฏิเสธ
- ไฟล์ใหญ่เกิน `PDF_MAX_BYTES` (8 MB) ถูกตัดจาก `content-length` **ก่อนอ่าน body**
  (เจอจริง: `info.dpt.go.th/.../2522update.pdf` = 165 MB)

**`DEAD_SOURCES` — โดเมนถูก แต่ที่อยู่เปิดไม่ได้** (`_lib/law-source.js` · `hostAllowed()` ตัดตั้งแต่ด่านแรก)
ต่างจากโดเมนนอกรายการตรงที่ด่านโดเมนปล่อยผ่าน แล้วไปตายตอน `fetch()` — เสียคำขอเพื่อได้ค่าว่าง
และผู้ใช้เห็นเหตุผลผิดว่า "ไม่อยู่ในโดเมนที่เชื่อถือได้" ทั้งที่โดเมนถูก แค่ที่อยู่นั้นเลิกใช้แล้ว

| ที่อยู่ | อาการ |
|---|---|
| `(www.)ratchakitcha.soc.go.th/DATA/PDF/…` | เว็บเวอร์ชันเก่า · 2026-08-17 กลายเป็นหน้า Cloudflare challenge · ฉบับเดียวกันเปิดได้ที่ `/documents/<id>.pdf` |
| `legal.labour.go.th` | ใบรับรอง SSL ยืนยันไม่ผ่าน — อาการเดียวกับ krisdika |

`web_search` ยังคืนที่อยู่รูปแบบเก่าอยู่เรื่อยๆ เพราะดัชนีเก่ายังไม่หมด · ข้อความที่คืนให้ผู้ใช้แยก 2 กรณีแล้ว
เพราะ "โดเมนไม่น่าเชื่อถือ" กับ "ที่อยู่นี้ตายแล้ว" แก้คนละแบบ — กรณีหลังตัวบทยังเปิดได้ แค่ต้องไปที่อยู่ใหม่

**ที่อยู่ดาวน์โหลดที่ไม่ลงท้ายด้วย `.pdf`** — `fetchPdfBase64` เดิมรับเฉพาะ URL ที่ลงท้าย `.pdf`
แต่เว็บหน่วยงานไทยจำนวนมากเสิร์ฟตัวบทผ่าน endpoint ที่มีแต่ query string
ยืนยัน 2026-08-17: `laws.anamai.moph.go.th/th/practices/download/?did=…&id=…` → `application/pdf` จริง
กฎเดิมจึงปิดตัวเองไม่ให้อ่านตัวบทของกรมอนามัยทั้งกรม — เป็นสาเหตุที่คำถามเรื่อง
เกณฑ์คุณภาพน้ำบริโภคตอบไม่ได้ **ทั้งที่ไฟล์เปิดได้** · ปลดล็อกแล้วได้ค่าครบทั้งตาราง
**ด่านจริงยังเป็น `content-type`** — หน้า HTML ที่ไม่ใช่ PDF ยังถูกตีกลับเหมือนเดิม

**`SOURCE_URL_RULES`** (`_lib/law-source.js` · ใส่ใน SYSTEM ของทั้ง `anchor-answer` และ `osh-law-relate`)
บอกโมเดลตั้งแต่รอบแรกว่าที่อยู่แบบไหนเปิดได้ — ถูกกว่าปล่อยให้คืนที่อยู่ที่ตายแล้วค่อยยิงซ้ำ
(ยิงซ้ำ = คำขอเพิ่ม 1 ครั้ง · ประโยคใน prompt = ฟรี) · อยู่ไฟล์เดียวกับ `DEAD_SOURCES` ตั้งใจ
prompt กับด่านตรวจต้องเปลี่ยนพร้อมกันเสมอ แยกกันแล้วมันจะเพี้ยนจากกันโดยไม่มีใครรู้

**`npm run seed:refs` — เติม cache ล่วงหน้า** (`scripts/seed-ref-answers.mjs`)
เวลาที่หายไปตอนสรุป 1 ฉบับไม่ได้หายไปกับตัวบทฉบับนั้น (ดึง + แตกข้อความ 6 หน้า < 2 วิ)
แต่หายไปกับการตามอ่านกฎหมายที่ถูกอ้างถึง ซึ่งวนอยู่กับเอกสารไม่กี่ฉบับ
เคสจริง: กฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560 มีคำถาม 8 ข้อ — 3 ข้อตอบด้วยเอกสาร 3 ฉบับ
(ควบคุมอาคาร ฉบับที่ 39 · ฉบับที่ 55 · ตรวจสุขภาพปัจจัยเสี่ยง 2563) เติมไว้ก่อนแล้วเหลือ 0 คำขอ
**ไม่ใช่การผ่อนด่านตรวจ** — สคริปต์เรียก `answerAnchoredQuestion` ตัวเดียวกับที่ระบบใช้ ผ่านด่านเดิมครบ
ที่ย้ายคือ "เวลาที่จ่าย" ให้ไปอยู่นอกคำขอของผู้ใช้ ไม่ใช่ "สิ่งที่ตรวจ"

**cache 180 วัน · 2 ตาราง**
- `lg_law_refs` — ผลรายฉบับ ผูกกับ `ref_key` (ชื่อกฎหมาย normalize แล้ว)
- `lg_ref_answers` — คำตอบ ผูกกับ `question_key` (คำถาม normalize แล้ว)
  ผูกกับ **คำถาม** ไม่ใช่ชื่อกฎหมาย เพราะกฎหมายฉบับเดียวตอบได้หลายคำถาม
  และคำถามเดียวกันจากกฎหมายคนละฉบับก็ใช้คำตอบเดิมได้
- เก็บผลที่ตอบไม่ได้ด้วย — ไม่งั้นรันซ้ำก็ยิง web_search ใหม่ทุกครั้งเพื่อได้คำตอบเดิมว่าตอบไม่ได้

**คีย์รอง `topic_key`** (mig 041) — `question_key` ชนเฉพาะเมื่อ **ตัวอักษร** ตรงกัน
ซึ่งแทบไม่เกิด เพราะคำถามถูกโมเดลเรียบเรียงใหม่ทุกรอบ:
"อาคารสำนักงานต้องมีที่ถ่ายอุจจาระ…กี่ที่" กับ "สำนักงานต้องจัดห้องน้ำห้องส้วมกี่ที่" คนละ key
→ จ่าย web_search + อ่านไฟล์ซ้ำเพื่อคำตอบเดิม

`topic_key = normalize(ชื่อกฎหมายที่อ้างถึง) + '|' + เรื่องที่ถาม`

| กติกา | เหตุผล |
|---|---|
| "เรื่อง" มาจากตารางคำที่คุมเอง (`TOPICS` ใน `ref-classify.js`) ไม่ใช่ตัดคำอัตโนมัติ | ภาษาไทยไม่เว้นวรรค เดาขอบเขตคำผิด = หยิบคำตอบของกฎหมายคนละเรื่องมาแสดง |
| เข้าเรื่องเดียวจึงออกคีย์ · เข้า 0 หรือ ≥2 เรื่อง = ไม่ออกคีย์ | "แสงสว่างในห้องน้ำ" คร่อม 2 เรื่อง ต้องค้นใหม่ ไม่ใช่เดาว่าเรื่องไหน |
| อ่านข้ามสำนวนรับเฉพาะ `status='answered'` ที่มี `source_excerpt` | `not_answered` ขึ้นกับสำนวนที่ใช้ค้น เปลี่ยนคำถามแล้วอาจเจอ — ปล่อยผ่านเท่ากับตรึงทุกสำนวนถัดไป 180 วัน |
| ชื่อกฎหมายสั้นกว่า 8 ตัวอักษร = ไม่ออกคีย์ | ระบุฉบับไม่ได้ชัด เสี่ยงชนข้ามฉบับ |

**คำตอบไปอยู่ "ใต้ข้อที่ถามถึง"** ไม่ใช่แถวใหม่ท้ายตาราง (`attachAnswers`)
จับคู่ด้วย `for_section` แล้วถอยไปหาจากชื่อกฎหมายในเนื้อข้อ · จับคู่ไม่ได้ก็ไม่ทิ้ง ไปแสดงในแผงรวมแทน

**และเขียนลง `req_text` เลย** (`mergeAnswerText`) ไม่ใช่เก็บไว้แสดงอย่างเดียว
เดิมคำตอบอยู่ใน `ref_answers` ซึ่งหน้าเว็บใช้แสดงผล กด "เพิ่มเข้าทะเบียน" แล้วทะเบียนได้แค่
"ต้องมีห้องน้ำ**ตามที่กฎหมายควบคุมอาคารกำหนด**" ไม่มีเลข 300 ตร.ม. ซึ่งเป็นตัวเลขเดียวที่ผู้ตรวจถามหา
— งานที่จ่ายเงินตามอ่านมาแล้วหายตอนบันทึก

| กติกา | เหตุผล |
|---|---|
| รวมเฉพาะ `status === 'answered'` | pending/not_answered ไม่มีเนื้อหาให้รวม · รวมไปก็เท่ากับเขียน "ยังไม่พบเกณฑ์" ลงทะเบียนกฎหมาย |
| เทียบแบบตัดช่องว่างก่อนต่อ | รันซ้ำ/กดลองใหม่ไม่เขียนซ้ำ |
| `REQ_TEXT_MAX` 900 ตัวอักษร เกินแล้วคงข้อความเดิม | ข้อเดียวอ้างหลายฉบับ (ข้อ 7 อ้าง 2 ฉบับ) รวมกันแล้วบานได้ · ข้อที่ยาวเกินคนอ่านไม่จบก็ไม่ต่างจากไม่มีข้อมูล |
| ฝั่งจอ: `RefAnswer` ไม่พิมพ์ `answer_plain` ซ้ำ · ช่องกรอกยืดตามความยาว (เพดาน 12 แถว) | เห็นสองรอบ / อ่านผ่านกล่อง 1–2 แถวแล้วตรวจงานไม่ได้ |

**ตัดรอบที่รู้อยู่แล้วว่าตอบไม่ได้** (`anchor-answer.js`) — รอบ 1 ตอบว่า "ให้เป็นไปตามตารางที่ 2"
แปลว่าตัวเลขอยู่ในตาราง ไม่ได้อยู่ในเนื้อความของข้อ · อ่านไฟล์รอบ 2 จึงได้ผลเดิมกลับมาแล้วต้องไปรอบ 3 อยู่ดี
`pointsToTable(out)` เป็นจริงเมื่อไร **ข้ามรอบ 2 เข้ารอบถอดตารางตรง ๆ**
ตัด 1 คำขอออกจากเส้นทางที่ยาวที่สุด ซึ่งเป็นตัวกำหนดเวลารวมของทั้งคลื่น (คลื่นยิงขนาน เวลารวม = จุดที่ช้าที่สุด)
ไฟล์ยังถูกดึงในรอบ 3 เหมือนเดิม หลักฐานจึงมาจากตัวบทจริงเท่าเดิม

**ด่านตัวเลขต้องนับ excerpt ของกฎหมายที่อ้างถึงด้วย** (`verify-numbers.js`)
ตั้งแต่ `mergeAnswerText()` เขียนเนื้อความจากกฎหมายที่อ้างถึงลง `req_text` ตัวเลขอย่าง
"300 ตารางเมตร" หรือ "ภายใน 30 วัน" มาจาก excerpt ของ **กฎหมายอีกฉบับ** ไม่ใช่ของข้อนั้น
ไม่รวมเข้าไปในหลักฐาน = ติดป้าย "ไม่พบในตัวบท" ให้ตัวเลขที่คัดมาจากตัวบทจริงทุกตัว
> เจอจริง: ข้อ 21 ขึ้นป้าย "⚠ ตรวจตัวเลข 30, 3 — ไม่พบในตัวบท" ทั้งที่ทั้งสองตัวมาจาก
> กฎกระทรวงตรวจสุขภาพฯ 2563 คำต่อคำ ("ภายในสามสิบวัน" · "ภายในสามวัน")
> สัญญาณเตือนปลอมแบบนี้อันตรายกว่าไม่มีป้าย เพราะทำให้ผู้ใช้เลิกเชื่อป้ายนี้ทั้งหมด รวมตอนที่มันถูก

**ด่านคุณภาพของหน้าที่ตัดมา ดูจำนวนอักขระไทย ไม่ใช่สัดส่วน** (`pdfPagesAround`)
เกณฑ์เดิม "ไทย ≥ 50%" เป็นเกณฑ์ของหน้าเนื้อความ ไม่ใช่ของตาราง — ตารางเกณฑ์มักมีชื่อ
พารามิเตอร์และวิธีวิเคราะห์เป็นอังกฤษเต็มคอลัมน์จนไทยเหลือต่ำกว่าครึ่ง แล้วถูกตีตกทั้งที่อ่านออกดี
พร้อมกันนั้นขยายระยะคั่นในการจับชื่อตารางจาก `\D{0,4}` เป็น `\D{0,10}`
เพราะ "บัญชี**หมายเลข** 1" มีอักษรคั่น 7 ตัว เกินเพดานเดิม
ทั้งสองจุดทำให้การตัดหน้าล้มแล้วถอยไปส่ง PDF ทั้งฉบับ ซึ่งย้อนกลับไปเสียทั้งเวลาและ token ที่ตั้งใจประหยัด

| ตาราง | ก่อนแก้ | หลังแก้ |
|---|---|---|
| ตารางที่ 2 (ห้องน้ำ) กฎกระทรวง 39 | ตัดได้ 3,514 tok | เท่าเดิม |
| ตารางที่ 3 (แสงสว่าง) กฎกระทรวง 39 | ตัดได้ 1,308 tok | เท่าเดิม |
| บัญชีหมายเลข 1 (น้ำบริโภค) กรมอนามัย | **ตัดไม่ได้ → ส่งทั้งฉบับ** | ตัดได้ 2,189 tok |

**งบเวลา** — ทุกขั้นตอนรู้เวลาที่เหลือ (`FN_BUDGET_MS` 255 วิ) หมดเวลาแล้ว **คืนผลที่ได้พร้อมบอกว่าอะไรยังไม่ได้ทำ**
(`skipped_for_time` · `skipped_for_quota`) ไม่ปล่อยให้ 504 ซึ่งเสียทั้งผลและเงินที่จ่ายไปแล้ว
ตัวเลขนี้ต้องขยับตาม `maxDuration` ใน `vercel.json` เสมอ

---

### P19 · ตรวจ Skill 3 ทั้งเส้นแล้วอุดรูที่ทำให้ผลขาดเงียบ ๆ (2026-08-21)

ตรวจโค้ดทั้งเส้นพร้อมยิงทดสอบเครือข่ายจริงกับทุกโดเมนที่ Skill 3 พึ่งพา
กลไก "ไปหาข้อมูล" · "เอาเฉพาะที่อ้างถึงโดยตรง" · "ถูกต้อง" ผ่านหมด — ที่พังคือ **ความครบ**

> #### ⚠ โควตากินกันเอง — การอ้างเจาะจงมาตราถูกเบียดตกได้ทั้งหมด
> `MAX_ANCHOR_LOOKUPS` เท่ากับ `MAX_TOTAL_LOOKUPS` (12) และ `whole_law` **หยิบก่อน**
> หยิบเต็ม 12 เมื่อไหร่ โควตาเหลือ 0 → ลูป `specific` ไม่ได้รันเลยสักรอบ
> แปลว่าการอ้างแบบเจาะจงมาตรา ซึ่ง**ตรงที่สุดและถูกที่สุด** ถูกการอ้างลอย ๆ เบียดตกหมด
> โดยไม่มีอะไรฟ้อง — เป็นผลขาดที่เงียบที่สุดในเส้นนี้
>
> **แก้:** `SPECIFIC_RESERVE` = 6 · กันโควตาให้ `specific` **เท่าที่มีจริง** ไม่ได้กันตายตัว
> ชั้น `whole_law`/`pending` มองเห็นแค่ `openBudget()` = `budget − reservedForSpecific`
> ไม่มี `specific` สักตัว → `whole_law` ยังได้เต็มเพดาน 12 เหมือนเดิมทุกประการ
>
> **และคืนโควตาให้รายการที่มาจาก cache** — cache hit ไม่ได้ยิงคำขอสักครั้ง
> `budget` คือเพดาน *ค่าใช้จ่าย* ไม่ใช่เพดานจำนวนรายการ ของฟรีจึงไม่ควรกินโควตา
> ข้อนี้คือสิ่งที่ทำให้คำแนะนำ "ลองสรุปซ้ำอีกครั้ง" เป็นจริง ไม่ใช่ได้ผลเดิมกลับมา
>
> | whole_law × specific | ก่อนแก้ | หลังแก้ |
> |---|---|---|
> | 12 × 8 | anchor 12 · **specific 0** | anchor 6 · specific 6 (+2 ค้าง) |
> | 12 × 0 | anchor 12 | anchor 12 (เท่าเดิม) |
> | 0 × 20 | specific 12 | specific 12 (เท่าเดิม) |

**บอกสาเหตุให้ตรง** — ฉบับที่ตกเพราะโควตาเคยถูกรายงานว่า *"เวลาในรอบนี้ไม่พอ"*
และไม่ถูกนับใน `skipped_for_time` ด้วย · ผู้ใช้กด "ลองใหม่" ก็ได้ผลเดิมเพราะแก้ผิดสาเหตุ
ตอนนี้แยก `skipped_for_quota` ออกมา โน้ตตรงตามเหตุจริง และ **`whole_law` ที่ไม่ได้ถามขึ้นแผงรวมด้วย**
(เดิมโผล่แค่ในตัวเลข ผู้ใช้รู้ว่าหายไป 6 จุดแต่ไม่มีทางรู้ว่าหายจุดไหน = ตามเองต่อไม่ได้)
ส่วน frontier ที่เหลือแต่ตัวซ้ำ/อ้างกลับมาที่ฉบับหลักเอง เลิกรายงานว่า "ยังไม่ได้ดึง" เพราะไม่จริง

**โดเมนที่ตายแล้ว** — `dlpw.go.th` ไม่มี A record ทั้ง apex และ `www` (อาการเดียวกับ krisdika)
ลิงก์บนโดเมนนี้ผู้ใช้เองก็เปิดไม่ได้ ปล่อยไว้เท่ากับยอมให้คำตอบติดที่มาเป็นลิงก์ตาย
ถอดออกจาก **3 ที่**: `PRIMARY_DOMAINS` · `ALLOWED_HOSTS` ใน `law-analyze.js` · ตัวเลือกวางลิงก์ในหน้าเว็บ
พร้อมแก้ `SOURCE_URL_RULES` ที่เคยแนะนำโมเดลให้ใช้โดเมนนี้แทน `legal.labour.go.th`
งานของกรมสวัสดิการฯ ครอบคลุมด้วย `labour.go.th` อยู่แล้ว (`osh.labour.go.th` ใช้ได้จริง)

**`PDF_FETCH_TIMEOUT` 45 → 20 วิ** — วัดจริง: ไฟล์ที่ดึงได้ใช้ 0.5–4.9 วิ
ส่วนโฮสต์ที่ส่ง body แบบ chunked แล้วไม่ยอมปิดสาย (`osh.labour.go.th` · `www.pcd.go.th`)
กินจนครบเพดานทุกครั้งโดยไม่ได้อะไรกลับมา = 45 วิจากงบ 255 วิต่อหนึ่งที่อยู่
20 วิยังเผื่อไว้ ~4 เท่าของเวลาที่ไฟล์จริงใช้ · ถ้าวันหนึ่งไฟล์ใหญ่จริงถูกตัดทิ้ง ให้ขยับทีละ 5 วิ **อย่ากลับไป 45**

**ยืนยันสดวันที่ตรวจ (2026-08-21):**

| ที่อยู่ | ผล |
|---|---|
| `ratchakitcha.soc.go.th/documents/<id>.pdf` | ✅ 200 · application/pdf · 26–85 KB · 0.5–4.9 วิ |
| `ratchakitcha.soc.go.th/documents/<id>` (ไม่มี .pdf) | ❌ 403 |
| `ratchakitcha.soc.go.th/DATA/PDF/...` | ❌ 403 / Cloudflare — อยู่ใน `DEAD_SOURCES` แล้ว |
| `legal.labour.go.th` · `krisdika.go.th` | ❌ ต่อไม่ติด — กันไว้แล้วทั้งคู่ |
| `dlpw.go.th` | ❌ ไม่มี A record → **ถอดออกรอบนี้** |
| `doeb.go.th` | ✅ 200 · 1.4 วิ (ผ่าน Imperva ได้ด้วย UA ของเรา) — เก็บไว้ |

**เก็บกวาด:** `fetchRelatedLaw` เคยประกาศ `web_search` tool ซ้ำเองแทนที่จะใช้ `WEB_SEARCH_TOOL`
ซึ่งเป็นการก๊อปที่หัวไฟล์เตือนไว้เองว่าห้ามทำ — เวอร์ชันของ tool หรือรายการโดเมนถูกแก้ที่เดียว
แล้วอีกเส้นยังค้นด้วยของเก่าโดยไม่มีใครรู้ · ตอนนี้ใช้ตัวเดียวกันทั้งสองเส้น

> **ค้างไว้พิจารณา:** `web_search_20250305` เป็นตัวแปรพื้นฐาน · `claude-sonnet-4-6` รองรับ
> `web_search_20260209` (dynamic filtering) แล้ว แต่ตัวใหม่รัน code execution อยู่เบื้องหลัง
> ซึ่งแลกเวลาในงบ 255 วิ — ยังไม่เปลี่ยนจนกว่าจะวัดผลจริงว่าคุ้ม

---

### P20 · ดัชนีราชกิจจานุเบกษาของเราเอง (2026-08-22)

**ปัญหา:** Skill 3 หา "ตัวบทของกฎหมายที่ถูกอ้างถึง" ด้วย `web_search` อย่างเดียว ซึ่งพัง 2 ทางพร้อมกัน
ดัชนีของ search engine ยังคืนที่อยู่ราชกิจจาฯ แบบเก่า `/DATA/PDF/...` ที่ตายแล้วอยู่เรื่อย ๆ (เสียคำขอฟรี)
และการ recheck จุด `pending` เสีย 1 คำขอ AI ต่อ 1 จุด จึงตกโควตาประจำ แล้วคืนว่า "ยังไม่ยืนยัน"
ทั้งที่ประกาศออกมาแล้ว

**เว็บราชกิจจาฯ ค้นเองไม่ได้** — ยิงจริง 2026-08-22 ทุก path ยกเว้น `/documents/<id>.pdf`
คืน 403 พร้อมส่วนหัว `cf-mitigated: challenge` รวม `/search` · `/api/search` · `/robots.txt` · `/sitemap.xml`
มีแค่ apex กับ `www` ไม่มี subdomain API ซ่อนอยู่ (`api.` · `backend.` · `search.` ไม่มี A record)
แปลว่า **รู้เลขเอกสารแล้วดึงไฟล์ได้ แต่หาเลขเอกสารเองจากเว็บไม่ได้**

**แต่ สลค. เปิดข้อมูลชุดเดียวกันไว้ 2 ทาง ซึ่งไม่มี Cloudflare ขวาง**

| ทาง | ที่อยู่ | ต้องใช้ Token | ขอบเขต |
|---|---|---|---|
| dump JSON รายเดือน | `soc.gdcatalog.go.th` ชุด `dataset_02_04` | ไม่ต้อง | **มิ.ย. 2566 เป็นต้นมา** |
| API | `api.soc.go.th/webservice/api/rkjs/{page}/{limit}?title=&type=&bookNo=&part=&dateBegin=&dateEnd=` | ต้อง (ขอที่ `www2.soc.go.th`) | ทั้งคลัง |

ยิง API เปล่า ๆ ได้ **401 ไม่ใช่ 403** — เปิดอยู่จริง ไม่มี Cloudflare แค่ต้องมีคีย์
ตอนนี้ใช้ทางแรกก่อน · คอลัมน์ `source` (`gdcatalog` | `soc_api`) เผื่อเติมทางที่สองทับได้โดยไม่ชนกัน

**`lg_gazette_index`** (mig 042) · `scripts/load-gazette-index.mjs` · `npm run load:gazette`
โหลดจริง 2026-08-22: อ่าน 35 เดือน **122,633 รายการ → เก็บ 18,282** (ประเภท ก 1,482)
ช่วง 2023-06-01 → 2026-04-22 · ฟิลด์ `URL` เป็นรูปแบบ `/documents/<id>.pdf` ครบ
ซึ่งเป็นที่อยู่เดียวที่ยังเปิดได้จริง — ดัชนีนี้จึง**การันตีลิงก์ ไม่ใช่เดาเอา** (สุ่มเปิด 3 ฉบับ ได้ 200 ทั้งหมด)

**ดัชนีมีแค่ชื่อเรื่อง ไม่มีเนื้อหา** — ใช้หาว่า "ฉบับไหนมีอยู่ และไฟล์อยู่ที่ไหน" เท่านั้น
ผลที่ได้เข้า prompt ในฐานะ *รายชื่อผู้ต้องสงสัย* ไม่ใช่คำตอบ · ด่าน `source_excerpt` ยังบังคับครบ
สิ่งที่ตัดออกคือ "รอบเดาว่าไฟล์อยู่ที่ไหน" ไม่ใช่ "รอบตรวจว่าตัวบทเขียนว่าอะไร"
เสียบเป็น **รอบ 0** ของ `answerAnchoredQuestion` · `recheckPending` ส่ง `gazette_parent` = ชื่อกฎหมายแม่เข้าไป
วัดจริงกับ Supabase: **133–448 ms ต่อการค้น 1 ครั้ง · 0 คำขอ AI**

**การจับคู่ — ประกาศลูกเก็บ "ส่วนท้าย" ของชื่อแม่ไว้ ไม่ใช่ส่วนหน้า**
```
แม่  กฎกระทรวง**ควบคุมสถานประกอบกิจการที่เป็นอันตรายต่อสุขภาพ** พ.ศ. 2560
ลูก  ประกาศ กสธ. เรื่อง หลักเกณฑ์…**ในสถานประกอบกิจการที่เป็นอันตรายต่อสุขภาพ** พ.ศ. 2564
```
คำขึ้นต้น "ควบคุม" ไม่ติดไปกับลูก แต่ส่วนท้ายติดเสมอ → `gazetteNeedles()` คืนส่วนท้ายยาวลดหลั่น
(แกนเต็ม → 34 → 26 → 20 ตัวอักษร) ลองจากยาวไปสั้น หยุดที่ชั้นแรกที่เจอ

| กติกา | เหตุผล (วัดกับดัชนีจริง 18,282 รายการ) |
|---|---|
| แกนเต็มเข้ารายการเสมอ ไม่ว่าสั้นแค่ไหน | `"ควบคุมอาคาร"` ยาว 11 ตัวอักษร — ตัดทิ้งเพราะสั้น แล้ว พ.ร.บ.ควบคุมอาคาร จะค้นไม่ได้เลย |
| เข็มสั้นกว่า `NEEDLE_MIN_ALONE` (18) ใช้ลำพังไม่ได้ | `"การสาธารณสุข"` (12) คืน **189 ฉบับ** ซึ่งเป็นประกาศหลักประกันสุขภาพแห่งชาติเกือบทั้งหมด |
| **มีเรื่องมาบีบแล้วไม่เจอ = จบ ห้ามถอยไปค้นกว้าง** | พ.ร.บ.ความปลอดภัยฯ + เรื่อง "ตรวจสุขภาพ" ไม่เจอ แล้วถอยไปค้นกว้างได้ประกาศกองทุนฯ 17 ฉบับ ที่ไม่ตอบอะไรเลย — ยัดเข้า prompt แย่กว่าไม่ให้อะไร เพราะไปเบียดที่ของ `web_search` |
| เข็มเรื่องใช้ **ข้อความไทยที่ match จริง** ไม่ใช่สลัก | `topicMatchText()` — ชื่อเรื่องในราชกิจจาฯ ใช้สำนวนเดียวกับตัวบท ส่วน `normalizeTopicKey()` คืน `healthcheck` ซึ่งไม่มีวันตรงกับชื่อไทย |
| `normalizeGazetteTitle()` อยู่ไฟล์เดียวใช้ทั้งฝั่งโหลดและฝั่งค้น | ก๊อปสองที่แล้วมันจะค่อย ๆ ต่างกัน แล้วเข็มค้นจะไม่มีวันตรงกับที่เก็บไว้ **โดยไม่มี error ฟ้อง** |

**ผลกับเคสกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560** — จุด `pending` ข้อ 22 วรรคสอง
ซึ่งรอบก่อนตอบว่า "ยังไม่ยืนยัน" เพราะโควตาหมด ตอนนี้ดัชนีคืน
*ประกาศ กสธ. เรื่อง หลักเกณฑ์และมาตรการควบคุมป้องกันเหตุรำคาญฯ (กิจการรังนกแอ่น) พ.ศ. 2568*
พร้อมลิงก์ที่เปิดได้ ใน 448 ms โดยไม่เสียคำขอสักครั้ง

> **ข้อจำกัดที่ต้องรู้ — ดัชนีนี้เสริม `web_search` ไม่ได้แทน**
> - dump เริ่มที่ **มิ.ย. 2566** · ประกาศลูกของกฎกระทรวงฉบับนี้ที่ออกปี 2561/2564/2565 **ไม่มีในดัชนี**
>   ต้องเติมด้วย API (ต้องมี Token) ถึงจะครบ
> - จับคู่ได้เฉพาะลูกที่ **เอ่ยชื่อเรื่องของแม่ตามลำดับคำเดิม** · ฉบับที่เรียบเรียงใหม่
>   ("…อันตรายต่อสุขภาพจากการประกอบกิจการรังนกแอ่น") ต้องอาศัยเข็มเรื่องมาช่วย
> - อัปเดตเดือนละครั้ง ไม่ใช่ real-time
> - สัญญาอนุญาตของชุดข้อมูลระบุว่า "อื่นๆ" ไม่ได้บอกชัด
> - `--force` กับด่าน `MIN_ROWS` 8,000 กันกรณี สลค. เปลี่ยนโครงสร้างแล้ว upsert ทับด้วยชุดที่ขาด
>   (เจอจริงรอบแรก: 2 เดือนห่อไว้เป็น `{"Worksheet":[...]}` แทน array ตรง ๆ ถูกข้ามเงียบ ๆ ไป 7,622 รายการ)

---

### P21 · ดัชนีโตเองจากสิ่งที่ระบบเปิดอ่านสำเร็จ (2026-08-22)

**ปัญหา:** ดัชนี P20 เริ่มที่ มิ.ย. 2566 · ทดสอบกับเป้าหมายจริง 11 ฉบับของกฎกระทรวงควบคุมสถานประกอบกิจการฯ 2560 **อยู่ในดัชนีแค่ 3** ที่เหลือประกาศก่อนหน้านั้นทั้งหมด

**แก้ที่ต้นทางไม่ได้** — ไล่ดูชุดข้อมูลของ สลค. บน GD Catalog หมดแล้ว มีชุดกฎหมายชุดเดียว (`dataset_02_04`) และไฟล์เก่าสุดคือเดือนนั้นจริง (`dataset_02_02` ว่าง · `dataset_02_03` เป็นมติ ครม.)

> **ทางลัดที่ลองแล้วไม่ผ่าน — เลขเอกสารถอดรหัสได้ แต่ใช้ได้แค่ 3 เล่ม**
> `142D041S0000000010900` = เล่ม 142 · ง · ตอน 041 · พิเศษ · หน้า 109
> สูตร `{เล่ม:3}{ก=A ข=B ค=C ง=D}{ตอน:3}{N ปกติ|S พิเศษ}{หน้า×100:13}` — ตรง **2,395 จาก 2,421**
> ถ้าใช้ได้ทั่วไปจะแปลว่าสร้าง URL เองได้จากเลขอ้างอิงที่กฎหมายทุกฉบับเขียนไว้ ไม่ต้องมีดัชนีเลย
> แต่สร้างของฉบับเก่า 5 ฉบับแล้ว **404 ทั้งหมด** (เล่ม 125 · 134 · 135 · 137 · 138)
> เลขแบบถอดรหัสได้มีเฉพาะ **เล่ม 140–142** และเล่ม 143 กลับไปใช้เลขทึบแล้ว

**ของใหม่: เลิกทิ้งสิ่งที่ระบบหามาได้เอง** (mig 043 · `source='discovered'`)
ทุกครั้งที่ Skill 3 ตอบสำเร็จ มันเพิ่งเปิดไฟล์นั้นอ่านจริงมาแล้ว = มีคู่ (ชื่อกฎหมาย, URL ที่เปิดได้) อยู่ในมือ
เดิมเก็บแค่คำตอบลง `lg_ref_answers` แล้วทิ้งที่อยู่ตัวบท · ตอนนี้เขียนกลับเข้าดัชนีด้วย
เสียบ 2 จุด: `answerAnchoredQuestion` (หลังผ่านด่านตรวจครบ) · `fetchRelatedLaw` (เมื่อ `status='resolved' && read_ok`)
ดัชนีจึงโตไปตามเอกสารที่ทะเบียนอ้างถึงจริง ซึ่งเป็นชุดเล็กและวนซ้ำ และครอบคลุมฉบับก่อน มิ.ย. 2566 ได้เอง

`npm run backfill:gazette` เติมย้อนหลังจาก `lg_ref_answers` · `lg_law_refs` · `lg_laws`
รันจริง: อ่าน 38 แถว → **เข้าดัชนี 18** (ไม่มี URL 10 · โดเมนไม่ผ่าน 1 · ไม่ใช่ตัวเอกสาร 6 · ซ้ำ 3)

**`looksLikeDocumentUrl()` — ด่านที่ต้องมี** เจอจริงตอน backfill รอบแรก 6 จาก 24 ไม่ใช่ตัวเอกสาร
| ที่อยู่ที่ถูกตัด | คืออะไร |
|---|---|
| `laws.anamai.moph.go.th/th/doh-annuance?page=4` | หน้าสารบัญ หน้าที่ 4 |
| `www.ratchakitcha.soc.go.th` | โดเมนเปล่า |
| `…/5937.html และ https://…/5951.html` | สอง URL ในสตริงเดียว |
| `laws.anamai.moph.go.th/th/news-anamai/215741` | หน้าข่าว ไม่ใช่ตัวบท (มีเลขรายการจึงรอดด่านอื่น) |

ปล่อยเข้าไปแล้วรอบหลังระบบจะส่งโมเดลไปเปิดหน้าสารบัญ **พร้อมป้ายว่า “ลิงก์นี้ยืนยันแล้วว่าเปิดได้”** ซึ่งแย่กว่าไม่มีลิงก์ เพราะมันดูน่าเชื่อ

> #### ⚠ แถว discovered เชื่อได้ไม่เท่าแถว gdcatalog — ต้องบอกโมเดลให้รู้
> แถวจาก สลค. มาจากสารบัญทางการ ชื่อเรื่องกับไฟล์จับคู่ถูกแน่นอน
> ส่วนชื่อเรื่องของแถว discovered คือสิ่งที่ **โมเดลตั้งให้ตอนตอบคำถามครั้งก่อน** ซึ่งพลาดได้
> เจอจริง: ไฟล์ `mr35-33-upd69.pdf` (กฎกระทรวง **ฉบับที่ 33**) ถูกตั้งชื่อว่า “ฉบับที่ 39”
> `formatGazetteHits()` จึงติดป้ายที่มารายแถว และ `formatGazetteHits` ตัดชื่อซ้ำออกก่อนเข้า prompt
> (ฉบับเดียวกันมักถูกเก็บหลาย URL — ต้นฉบับราชกิจจาฯ + ฉบับรวมของ ASA — ยัดไปทั้งคู่ = เสียคำขอเปิดไฟล์เดียวกันซ้ำ)

**ตาข่ายรองเฉพาะแถว discovered** — ชื่อกฎหมายไทยแทบไม่บอก “เรื่อง” ไว้ในชื่อ เรื่องอยู่ในเนื้อความ
> “กฎกระทรวง ฉบับที่ 55 (พ.ศ. 2543) ออกตามความในพระราชบัญญัติควบคุมอาคาร” มีชื่อกฎหมายแม่ครบ
> แต่ไม่มีคำว่า “บันไดหนีไฟ” ทั้งที่เป็นฉบับที่ตอบคำถามนั้น

บังคับให้เจอเรื่องในชื่อเรื่องด้วยจึงตัดฉบับที่ถูกต้องทิ้งประจำ — วัดจริงได้ 1 จาก 4
จึงยอมให้ค้นด้วยชื่อกฎหมายแม่อย่างเดียว **แต่จำกัดที่ `source='discovered'` · เข็มชั้นแรกเท่านั้น · เพดาน 4 แถว**

> **เข็มชั้นแรก (แกนเต็ม) เท่านั้น — เปิดให้ชั้นรองด้วยแล้วพัง**
> รอบแรกยอมให้ลองเข็มชั้นรองด้วย ผลคือคลื่น 12 จุดของกฎกระทรวงฯ 2560 ขึ้น **"เจอ 12/12"**
> ซึ่งดูดีมาก แต่เปิดดูจริงพบว่า **8 จุดได้ประกาศเรื่องเสียงกับเรื่องแมลงกลับมาเหมือนกันหมด**
> ทั้งที่ถามเรื่องระยะห่าง บ่อดักไขมัน น้ำบริโภค — เพราะเข็มชั้นรอง `"เป็นอันตรายต่อสุขภาพ"` (20)
> ไปตรงกับประกาศลูก *ทุกฉบับ* ของแม่เดียวกัน โดยไม่เกี่ยวกับเรื่องที่ถามเลย
>
> เป็นความผิดพลาดแบบเดียวกับที่ P17 ตั้งใจแก้ (คืนข้อที่ไม่เกี่ยวมาแทนคำตอบ) แค่ย้ายมาเกิดเร็วขึ้นหนึ่งชั้น
> **"เจอ" ที่ผิดฉบับแย่กว่า "ไม่เจอ"** เพราะไปเบียดที่ของ `web_search` และมาพร้อมป้ายว่าระบบเคยเปิดอ่านสำเร็จ
>
> แกนเต็มไม่มีปัญหานี้: `"ควบคุมสถานประกอบกิจการที่เป็นอันตรายต่อสุขภาพ"` ไม่ตรงกับชื่อประกาศเรื่องเสียง
> แต่ `"ควบคุมอาคาร"` ตรงกับ *"กฎกระทรวง ฉบับที่ 39 … ออกตามความในพระราชบัญญัติควบคุมอาคาร"*
> ซึ่งเป็นเคสที่ตาข่ายรองมีไว้เพื่อช่วยจริง ๆ · **หลังแก้: 8/12 และฉบับที่คืนมาตรงเรื่อง**
> 4 จุดที่เหลือ (ระยะห่าง · บ่อดักไขมัน · ตรวจสุขภาพเพิ่ม · น้ำบริโภค) ตกไป `web_search` ตามเดิม
> ซึ่งถูกต้องแล้ว เพราะฉบับจริงของจุดเหล่านั้นยังไม่อยู่ในดัชนี
ทำไมผ่อนได้เฉพาะกลุ่มนี้: กฎ “ห้ามถอยไปค้นกว้าง” ของ P20 ตั้งไว้เพราะค้นกว้างในก้อน 18,000 แถว
คืนกฎหมายลูกฉบับอื่นมาเป็นพรวน · แต่แถว discovered คือชุดที่ระบบเคยเปิดอ่านเพราะมันเกี่ยวข้องจริง
**ต่างกันที่ชนิดของข้อมูล ไม่ใช่ที่ความเข้มของกฎ** · หลังแก้ได้ 5 จาก 6 และเคส regression
(พ.ร.บ.การสาธารณสุข ไม่บีบเรื่อง) ยังคืนค่าว่างเหมือนเดิม

ผลที่ยิงตาข่ายรองจะขึ้น prompt คนละข้อความ — บอกตรง ๆ ว่า *“ฉบับที่ออกตามกฎหมายแม่เดียวกัน อาจไม่ใช่ฉบับที่ตอบคำถามนี้”*
ไม่ใช่ “ยืนยันแล้ว ให้ใช้ก่อน” ซึ่งจะชวนให้โมเดลยึดไฟล์ผิดฉบับด้วยความมั่นใจ

**ยังไม่ได้ทำ:** ด่านฉบับที่ถูกแก้/ยกเลิก ปัจจุบันมีแค่ประโยคใน prompt (`anchor-answer.js:68` · `osh-law-relate.js:82`)
ไม่มีด่านฝั่งโค้ด · ในดัชนีมีฉบับแก้ไขเพิ่มเติม 2,269 รายการและที่ชื่อเรื่องบอกว่ายกเลิกอีก 183 รายการ
ทำเป็นด่านเตือนได้ แต่ยังบอกไม่ได้ว่าฉบับไหนแก้ฉบับไหน เพราะข้อมูลนั้นอยู่ในเนื้อความ ไม่ใช่ในชื่อเรื่อง

**แหล่งอื่นที่ตรวจแล้ว (2026-08-22)** — `api.soc.go.th` คืน 401 ไม่ใช่ 403 = เปิดอยู่จริง ย้อนได้ทั้งคลัง
ต้อง Bearer Token ขอที่ `www2.soc.go.th` ซึ่ง**ติด Cloudflare** ต้องเปิดในเบราว์เซอร์เอง สคริปต์ทำแทนไม่ได้ ·
`law.go.th` (ระบบกลางทางกฎหมาย) เว็บใช้ได้แต่ API `apig.law.go.th` ปิด (403 ทุก path ที่ลอง) ·
`krisdika.go.th` · `dl.parliament.go.th` ต่อไม่ติดทั้งคู่ · เว็บหน่วยงานยังดีที่สุดรองจากนี้:
`laws.anamai.moph.go.th` มีสารบัญครอบคลุม **พ.ศ. 2535 → 2568** (72 ฉบับใน 2 หมวด) ดึงได้ฟรีไม่ต้อง Token

---

### P22 · บทยกเลิก — อ่านแล้วไปเช็คกับทะเบียนของเราเอง (2026-08-22)

**ปัญหา:** Skill 1+2 สกัด `repeals[]` จากบท "ให้ยกเลิก…" อยู่แล้ว แต่ส่งกลับไปแค่ชื่อ
ให้หน้าเว็บจับคู่กับทะเบียนเอง ผลคือ
- ผลจับคู่ไม่ติดไปกับคำตอบของ API — หน้าอื่นหรือ staging ที่กินผลเดียวกันไม่รู้เรื่องด้วย
- รายการกฎหมายในหน้าโหลดไม่ทัน = ขึ้น "ไม่พบในทะเบียน" ทั้งที่มีอยู่

**ของใหม่:** `api/_lib/repeal-match.js` · `matchRepeals()` อ่าน `lg_laws` แล้วเติมผลจับคู่ฝั่ง server
คืน array ขนาดเท่าเดิมเสมอ — ทุกฉบับที่ตัวบทสั่งยกเลิกอยู่ในผลลัพธ์ ไม่ว่าจับคู่ได้หรือไม่

> **บทยกเลิกต้องขึ้นเหมือนกันทั้งกรณีพบและไม่พบในทะเบียน**
> มันเป็นข้อเท็จจริงที่เขียนอยู่ในตัวบท ไม่ได้ขึ้นกับว่าเราเคยบันทึกฉบับเก่าไว้หรือเปล่า
> ตัดทิ้งเพราะจับคู่ไม่ได้ = ผู้ตรวจ ISO ไม่มีทางรู้ว่าฉบับนี้ไปยกเลิกอะไรไว้
> ต่างกันแค่ `in_registry` ซึ่งเป็นตัวบอกว่ามีปุ่ม "ตั้งเป็นยกเลิกเลย" ให้กดหรือต้องบันทึกเองก่อน

แยก `registry_checked` ออกจาก `in_registry` — "อ่านทะเบียนไม่ได้" กับ "ตรวจแล้วไม่พบ"
คนละเรื่องและแก้คนละทาง · ยุบรวมเป็น false เหมือนกันจะทำให้ผู้ใช้เข้าใจผิดว่าตรวจแล้ว

> #### ⚠ ปีต่างกัน = คนละฉบับ · จับคู่แบบมั่นใจไม่ได้เด็ดขาด
> `baseName()` ตัด "พ.ศ. NNNN" ทิ้งก่อนเทียบ ซึ่งจำเป็นสำหรับชื่อที่สะกดต่างกันเล็กน้อย
> แต่มันทำให้ฉบับเก่ากับฉบับใหม่ของเรื่องเดียวกัน **เหมือนกันเป๊ะหลังตัดปี**
>
> เจอจริงตอนทดสอบกับทะเบียน 160 ฉบับ:
> ```
> ตัวบทสั่งยกเลิก  กฎกระทรวงกำหนดหลักเกณฑ์…ควบคุมสถานประกอบกิจการฯ พ.ศ. 2545
> จับคู่ไปโดน      LA-005 กฎกระทรวง ควบคุมสถานประกอบกิจการฯ พ.ศ. 2560  (ยัง active)
> sim = 0.75 พอดี → ผ่านเกณฑ์ → ขึ้นปุ่ม "ตั้งเป็นยกเลิกเลย"
> ```
> กดแล้วจะไปยกเลิก **ฉบับปัจจุบัน** แทนฉบับเก่า = ทะเบียนผิดทันที และเป็นความผิดพลาด
> ที่ผู้ใช้จับไม่ได้ เพราะระบบเป็นฝ่ายเสนอให้กดเอง
>
> `yearsConflict()` ตัดสิทธิ์ก่อนเกณฑ์ `sim` เสมอ · ทั้งสองฝั่งมีปีและปีไม่ตรง = คนละฉบับแน่นอน
> ยังแสดงผ่าน `near_miss_year` ให้เห็นว่าเจออะไรใกล้เคียง (จะได้ไม่ต้องไปหาเอง) แต่ไม่มีปุ่มให้กด
> เทียบเลขไทยกับอารบิกได้ ("พ.ศ. ๒๕๖๐" = "พ.ศ. 2560")

ตัวเทียบชื่อฝั่ง server ต้องให้ผลเดียวกับ `findLawDuplicate()` ใน `src/lib/ui.jsx` เป๊ะ
หน้าเว็บยังมีของตัวเองไว้ใช้เมื่อ API ไม่ได้ส่ง `registry_checked` มา (ผลจาก cache รุ่นเก่า)
สองฝั่งเพี้ยนจากกันเมื่อไหร่ ผู้ใช้จะเห็นผลจับคู่ต่างกันในหน้าเดียวกันโดยไม่มีอะไรฟ้อง

---

### P23 · ลิงก์ราชกิจจาฯ ต้นฉบับติดไปกับคำตอบ (2026-08-22)

**ที่ผู้ใช้ขอ:** จุดที่ดัชนีหาไม่เจอให้ไปค้นเว็บอื่นตามเดิม แต่ขอให้**ลิงก์ราชกิจจาฯ ออกมาด้วย**
เพราะเป็นที่มาที่ผู้ตรวจ ISO ยอมรับ ส่วนเว็บหน่วยงานหรือฉบับรวมขององค์กรวิชาชีพเป็นแหล่งรอง

**`resolveGazetteLink()`** เติมฟิลด์ `gazette_url` + `gazette_ref` (เล่ม/ตอน/หน้า) ให้คำตอบ

> #### ⚠ เป็น "ของแถม" ไม่ใช่ "ของแทน" — ห้ามสลับ `source_url`
> ฉบับรวมมักรวมฉบับแก้ไขไว้แล้ว ส่วนต้นฉบับราชกิจจาฯ เป็นตัวก่อนแก้
> เคสจริงของระบบนี้: ตารางที่ 2 ของกฎกระทรวง ฉบับที่ 39 ถูกยกเลิกโดยฉบับที่ 63
> **อ่านตัวเลขจากฉบับรวมของ ASA แล้วอ้างลิงก์ราชกิจจาฯ ฉบับ 39 = อ้างเอกสารที่ไม่มีตัวเลขนั้นอยู่จริง**
> หลักฐาน (`source_url`) ต้องอยู่ที่เดิมเสมอ · ลิงก์ราชกิจจาฯ ขึ้นเป็นลิงก์ที่สองข้าง ๆ กัน

**ด่านตรวจ 3 ชั้นก่อนยอมพิมพ์ลิงก์ออกไป** — ทั้งสามชั้นมาจากของที่พังจริงตอนทดสอบ

| ชั้น | กันอะไร | หลักฐาน |
|---|---|---|
| รับเฉพาะ `gdcatalog`/`soc_api` | แถว `discovered` จับคู่ชื่อกับ URL ผิดได้ เพราะชื่อมาจากโมเดล | แถวชื่อ *"ประกาศ กสธ. ค่ามาตรฐานมลพิษทางเสียงฯ พ.ศ. 2561"* ชี้ไป `142D051S…157.pdf` — เปิดดูจริงเป็น **"ข้อบัญญัติ อบต.หาดส้มแป้น พ.ศ. 2565"** |
| ปีต้องตรง (`yearsConflict`) | ฉบับก่อน/หลังแก้ไขของเรื่องเดียวกัน | ตัวเดียวกับที่ใช้ในบทยกเลิก (P22) |
| เลข "ฉบับที่ N" ต้องตรงเป๊ะ | substring bleed | เข็ม `"ฉบับที่ 39"` ไปตรงกับ **"กฎกระทรวง ฉบับที่ 399 (พ.ศ. 2568) ว่าด้วยการยกเว้นรัษฎากร"** |

**ผลจริงตอนนี้: 1 จาก 5** — ฉบับที่ resolve ได้คือ *ควบคุมป้องกันเหตุรำคาญฯ 2568*
(`documents/71256.pdf` · เล่ม 142 ตอนที่ 204 หน้า 40) ส่วนอีก 4 ฉบับเป็นของก่อน มิ.ย. 2566
จึงยังไม่มีในสารบัญทางการ · **คืน `null` ดีกว่าเดา** — ลิงก์ราชกิจจาฯ ที่ผิดฉบับอันตรายกว่าไม่มีลิงก์
เพราะมันดูเป็นทางการจนไม่มีใครไปตรวจซ้ำ · ตัวเลขนี้จะดีขึ้นเองเมื่อได้ Token ของ สลค. มาเติมย้อนหลัง

**เก็บกวาด:** ลบแถว `discovered` ที่พิสูจน์แล้วว่าจับคู่ผิด (`142D051S…157.pdf`) ออกจากดัชนี

---

### P24 · ตรวจของจริงในดัชนี แล้วอุดที่ต้นทาง (2026-08-22)

**เปิดไฟล์ทุกแถว `discovered` มาตรวจว่าเนื้อหาตรงกับชื่อที่เก็บไว้จริงไหม — 4 จาก 17 เสีย**

| อาการ | ตัวอย่างที่เจอ |
|---|---|
| ลิงก์ตาย | `ratchakitcha.soc.go.th/documents/2556E185P042.pdf` → **404** · `rd.go.th/publish/5951.0.html` → 500 · `epa.onep.go.th/…` → ต่อไม่ติด |
| จับคู่ชื่อกับไฟล์ผิด | ชื่อ *"กฎกระทรวง ฉบับที่ 39"* ชี้ไป `mr35-33-upd69.pdf` ซึ่งเปิดดูแล้วเป็น **ฉบับที่ 33** |

**อุดที่ต้นทาง: `rememberGazetteDoc()` รับเฉพาะฉบับที่ "เปิดไฟล์อ่านจริงในรอบนี้"**
เส้นทาง `firstRoundComplete` ตอบจาก snippet ของผลค้นล้วน ๆ ชื่อกฎหมายกับ URL จึงมาคนละแหล่ง
ไม่มีอะไรยืนยันว่าคู่กันถูก — คู่ที่ผิดในดัชนีมาจากเส้นทางนี้ทั้งหมด
ตอนนี้ `anchor-answer` ส่ง `opened: !skippedRead && !!pdfB64` · `osh-law-relate` ส่ง `opened: true` (มี `read_ok` อยู่แล้ว)

**`npm run verify:gazette`** ตรวจย้อนหลัง · `--prune` ลบเฉพาะแถวที่ **ลิงก์ตาย** เท่านั้น
"เนื้อหาไม่ตรง" แค่รายงาน ไม่ลบอัตโนมัติ เพราะยังมีโอกาสเป็น false alarm — ลบจากการเดาแล้วทิ้งของถูก
แย่กว่าปล่อยของผิดไว้แล้วมีคนเห็นรายงาน

> #### ⚠ ตัวตรวจเองก็ต้องทนฟอนต์เพี้ยน ไม่งั้นได้ false alarm เกือบหมด
> รอบแรกตรวจแบบเทียบตรง ๆ ได้ "ไม่ตรง 5 จาก 17" ซึ่ง **ผิด 4 ใน 5**
> ไฟล์ ASA/ราชกิจจาฯ ฝังฟอนต์ที่ทำวรรณยุกต์หายและ า เพี้ยนเป็น ำ:
> `"ฉบับที  39"` · `"ก ำหนดมำตรฐำนกำรตรวจสุขภำพ"` · `"คุณภาพสิ งแวดล้อม"`
> ต้องเทียบแบบตัด `[่-๎]` และ `[ำา]` ทิ้ง — เทคนิคเดียวกับ `pdfPagesAround`
>
> **แต่ตัดมากไปก็พลาดของจริง** — พอตัด `"ฉบับที่ N"` ออกจากคำหลักเพื่อกัน false alarm
> ตัวตรวจก็ปล่อยแถว `mr35-33-upd69.pdf` (ฉบับ 33 ที่ชื่อว่าฉบับ 39) ผ่านทันที
> จึงต้องแยกเป็น **สองด่านคนละชนิด**: ด่านชื่อเรื่อง (ทนฟอนต์) + ด่านเลขฉบับ (จับแบบ `ฉบับท\S{0,3}?\s*ตัวเลข`)
> และเลขฉบับต้องตรวจ **ก่อน** ด่านชื่อเรื่องเสมอ ไม่งั้นชื่อที่คร่อมสองฉบับจะตกไปกอง "ตรวจไม่ได้" ทั้งที่ผิดจริง
>
> หลังแก้: 9 ตรง · 1 ไม่ตรง (จับได้ถูกตัว) · 0 ลิงก์ตาย · 4 ชื่อสั้นเกินตรวจอัตโนมัติ

**เพิ่มโดเมน `moi.go.th`** — ทดสอบ 14 โดเมนที่ยังไม่อยู่ในรายการ แล้วยิงไฟล์จริงดู
มีตัวเดียวที่ให้ตัวบทระดับที่ใช้ได้: `law.moi.go.th/105.pdf` = **พ.ร.บ.ควบคุมอาคาร พ.ศ. 2522 ฉบับเต็ม
ของสำนักงานคณะกรรมการกฤษฎีกา** · 147,616 อักขระ · ไทย 90% · ผ่าน `pdfTextUsable` · มีมาตรา 8 ครบ
เป็นตัวบทชนิดเดียวกับที่ `krisdika.go.th` เคยให้ก่อนโดเมนตาย และมหาดไทยคือกระทรวงที่ออก
กฎกระทรวงควบคุมอาคารทุกฉบับ (39 · 55 · 63) ซึ่งเป็นฉบับที่ทะเบียนนี้อ้างถึงบ่อยที่สุด

ที่ทดสอบแล้ว **ไม่เพิ่ม**: `dla.go.th` (PDF ในดัชนีค้นหา 404 หมด) · `oap.go.th` (ได้คู่มือ ไม่ใช่ตัวบท)
· `industry.go.th` · `thailaws.com` · `tisi.go.th` · `oie.go.th` · `nhso.go.th` (ต่อไม่ติดทั้งหมด)
· `fda.moph.go.th` · `ddc.moph.go.th` (ครอบคลุมด้วย `moph.go.th` อยู่แล้ว)

> **ทำไมไม่ปล่อยให้ค้น Google แล้วรับทุกโดเมน** — `web_search` ค้นทั้งเว็บอยู่แล้ว
> ด่านโดเมนไม่ได้จำกัด "ที่ค้น" แต่จำกัด "ที่อ้างอิงได้" · ปล่อยผ่านหมดเท่ากับยอมให้ทะเบียนกฎหมาย
> อ้างบล็อกหรือสรุปของสำนักงานกฎหมายเอกชน ซึ่งผู้ตรวจ ISO ไม่รับ และไม่มีใครรับประกันว่าตรงฉบับ
> ทางที่ถูกคือ **เพิ่มโดเมนทีละตัวเมื่อยิงไฟล์จริงแล้วผ่าน `pdfTextUsable`** อย่างที่ทำกับ `moi.go.th`

---

### P25 · รอบกู้ — ค้นเว็บกว้างเพื่อ "ระบุตัวฉบับ" ไม่ใช่เพื่อเอาคำตอบ (2026-08-22)

**ปัญหา:** ด่าน (ก) ตัดผลที่ไม่ได้อยู่ในโดเมนที่เชื่อถือได้ทิ้งทั้งหมด ถูกต้องในแง่การอ้างอิง
แต่ทิ้ง **เบาะแส** ไปด้วย · บล็อกกฎหมาย สรุปของที่ปรึกษา เว็บข่าว มักบอกตรง ๆ ว่า
*"เกณฑ์นี้อยู่ในประกาศกระทรวง X เรื่อง Y พ.ศ. 2565 ข้อ 7 (ราชกิจจาฯ เล่ม 139 ตอนพิเศษ 12 ง)"*
นั่นไม่ใช่คำตอบ แต่มันคือ **ที่อยู่ของคำตอบ** ซึ่งพาเรากลับไปที่ตัวบทจริงได้

**`api/_lib/open-web-lead.js` · `findLawIdentity()`** — รอบ 5.5 ยิงเมื่อเส้นทางปกติไม่ได้ผล
(ไม่เจอ · โดเมนไม่ผ่าน · ไม่มีข้อความรองรับ) และเหลือเวลา > 60 วิ

```
เบาะแส → (ก) resolveGazetteLink()  → ลิงก์ราชกิจจาฯ
        → (ข) ดัชนี discovered
        → (ค) ค้นโดเมนที่เชื่อถือได้อีกรอบ ด้วย "ชื่อฉบับที่รู้แล้ว" ไม่ใช่คำถามลอย ๆ
        → เปิดไฟล์จริง → ผ่านด่านเดิมครบทุกด่าน
```

ขั้น (ค) คือจุดที่เบาะแสให้ผลจริงที่สุด — **เว็บหน่วยงานเก็บไฟล์ไว้ใต้ชื่อเรื่อง ไม่ได้เก็บไว้ใต้คำถามของผู้ใช้**
รอบแรกค้นด้วย *"สถานประกอบกิจการต้องมีบ่อดักไขมันเมื่อไร"* ไม่เจอ
รอบกู้ค้นด้วย *"ประกาศกระทรวงสาธารณสุข เรื่อง กำหนดสถานประกอบกิจการที่ต้องจัดให้มีบ่อดักไขมัน พ.ศ. 2565"* เจอ

> #### ═══ กติกาที่ห้ามละเมิด ═══
> เนื้อหาจากเว็บนอกรายการ **ห้ามกลายเป็น `answer_plain` หรือ `source_excerpt`**
> มันมีสิทธิ์เดียวคือบอกว่า "ให้ไปเปิดฉบับไหน" · เปิดไม่ได้ = ยัง `not_answered` เหมือนเดิม
>
> เหตุผล: ทะเบียนนี้ใช้ตรวจ ISO · ผู้ตรวจถามว่า "อ้างจากอะไร" แล้วคำตอบเป็นบล็อก = ใช้ไม่ได้ทันที
> และบล็อกไทยจำนวนมากคัดจากฉบับก่อนแก้ไขโดยไม่บอก ซึ่งอันตรายกว่าไม่มีข้อมูล

**"ถ้ามันถูกต้องก็ใช้เป็นคำตอบได้ไม่ใช่หรือ"** — ถูกในทางเนื้อหา แต่ *"ถ้ามันถูกต้อง"*
คือสิ่งที่พิสูจน์ไม่ได้ตอนผู้ตรวจถาม · ทางออกคือ `criteria_seen` — แสดงว่า **"เขาว่ากันว่าเท่าไร"**
พร้อมป้าย *"ยังไม่ยืนยันกับตัวบท ห้ามใช้อ้างอิงจนกว่าจะเปิดฉบับจริง"* + ชื่อแหล่ง + ระดับความตรงกัน
(หลายแหล่งตรงกัน / แหล่งเดียว / แหล่งขัดกัน) ให้ผู้ใช้ประเมินน้ำหนักเอง
สถานะยังเป็น `not_answered` จึง **โครงสร้างกันไว้แล้ว** ว่า `mergeAnswerText()` ไม่เอาไปเขียนลงทะเบียน
(กติกาเดิม: รวมเฉพาะ `status === 'answered'`)

กำไรแม้เปิดตัวบทไม่ได้: เปลี่ยน "ไม่พบ" (ตันสนิท) เป็น "คำตอบอยู่ในฉบับนี้ ข้อนี้ ไปเปิดต่อ"

> #### ⚠ "เอ่ยถึงกฎหมาย" กับ "เป็นกฎหมายฉบับนั้น" คนละเรื่อง
> `resolveGazetteLink('พระราชบัญญัติควบคุมอาคาร พ.ศ. 2522')` เคยคืน `documents/100682.pdf`
> ซึ่งเปิดดูแล้วเป็น **คำวินิจฉัยศาลรัฐธรรมนูญ ที่ 25/2568** ที่ชื่อเรื่องเอ่ยว่า
> *"เรื่อง พระราชบัญญัติควบคุมอาคาร พ.ศ. 2522 มาตรา 40 … ขัดหรือแย้งต่อรัฐธรรมนูญหรือไม่"*
> ด่านปีไม่จับ เพราะชื่อเรื่องมีปี 2522 ของกฎหมายที่ถูกอ้างอยู่ในนั้นเอง
>
> **แก้:** ชื่อฉบับต้องอยู่ *ต้นชื่อเรื่อง* (ยอมให้มีคำนำหน้าไม่เกิน 12 ตัวอักษร)
> ชื่อเรื่องของกฎหมายฉบับหนึ่งจะขึ้นต้นด้วยชนิดและชื่อของตัวมันเองเสมอ
> ส่วนเอกสารที่พูดถึงกฎหมายฉบับอื่นขึ้นต้นด้วยชนิดของตัวเอง แล้วค่อยเอ่ยถึงทีหลัง

**คลื่นดัชนี 12 จุดหลังเก็บกวาด: 6/12 และถูกต้องทุกตัว** (ก่อนหน้า 8/12 แต่ 2 ตัวเป็นฉบับผิด
ที่มาจากแถวเสียซึ่งลบไปแล้ว) · 6 จุดที่เหลือเข้ารอบกู้แทนที่จะตัน

---

### P26 · ปลดล็อกการค้น + ชั้นที่ 3 `.go.th` / `.or.th` (2026-08-22)

**คอขวดตัวจริงอยู่ที่ `WEB_SEARCH_TOOL`** — เดิมตั้ง `allowed_domains: TRUSTED_DOMAINS`
แปลว่าโมเดล **ค้นไม่เจอตั้งแต่ต้น** ไม่ใช่ "เจอแล้วถูกตัด" · ตัวบทที่ถูกเก็บไว้ที่เว็บหน่วยงาน
ซึ่งเราไม่ได้นึกถึงตอนเขียนรายชื่อ จะหายไปเงียบ ๆ และเราไม่มีทางรู้ว่าพลาดอะไรไป
รายชื่อตายตัวจึงกันขยะได้จริง แต่กันของดีไปด้วย

**ของใหม่: ปล่อยให้ค้นกว้าง แล้วบังคับที่ "ผลลัพธ์" แทน** — กรองที่ผลลัพธ์แม่นกว่ากรองที่คำค้น
เพราะเห็นของจริงแล้วค่อยตัดสิน

**ชั้นที่ 3 · `.go.th` / `.or.th`** (`isThaiOfficialHost` · `sourceTier`)

| ชั้น | คือใคร | ตอบได้ | ป้ายเตือน |
|---|---|---|---|
| `primary` | ราชกิจจาฯ · เจ้าของกฎหมาย (รายชื่อที่คัดเอง) | ✅ | — |
| `secondary` | ฉบับรวมองค์กรวิชาชีพ (asa · shawpat) | ✅ | ตรวจกับต้นฉบับก่อนอ้างอิง |
| `official` | **`.go.th` / `.or.th` อื่นทั้งหมด** | ✅ | เว็บบางแห่งเก็บฉบับก่อนแก้ไขโดยไม่บอก ควรเทียบกับราชกิจจาฯ |
| `none` | บล็อก · `.com` · `.org` · `.net` | ❌ | ใช้เป็นเบาะแสได้เท่านั้น (P25) |

`.go.th` ออกให้เฉพาะหน่วยงานรัฐไทย · `.or.th` เฉพาะองค์กรไม่แสวงกำไร/สมาคมวิชาชีพที่จดทะเบียน
**ตรวจได้จาก URL ตรง ๆ ไม่ต้องมีใครมาตัดสินว่า "น่าเชื่อถือไหม"** — เป็นกฎ ไม่ใช่ความเห็น
ต่างจาก `.com`/`.org` ที่ใครก็จดได้ (`fdasamutsongkhram.org` ตกด่านด้วยเหตุนี้ ไม่ใช่ `.or.th`)

ด่านหลักฐานไม่ผ่อนสักข้อ — ยังต้องดึงไฟล์มาอ่านจริงและคัด `source_excerpt` จากตัวบท
ชั้น `official` แค่ทำให้ "แหล่งที่มีตัวบทจริง" ไม่ถูกตัดทิ้งเพราะเราลืมใส่ชื่อโดเมนไว้

> #### ⚠ เปิดชั้น .go.th แล้วโดเมนตายกลับเข้ามาได้อีก
> `krisdika.go.th` กับ `dlpw.go.th` เคยถูกกันโดยปริยายเพราะไม่ได้อยู่ใน `TRUSTED_DOMAINS`
> พอ `.go.th` ผ่านอัตโนมัติ ทั้งคู่กลับมาผ่านด่านโดเมนได้ ทั้งที่ยิงจริงวันนี้ยัง `http=000` ทั้งคู่
> → ย้ายไปกันที่ `DEAD_SOURCES` แทน ไม่งั้นเสียคำขอไปกับการดึงของที่ดึงไม่ได้
> แล้วผู้ใช้เห็นเหตุผลผิดว่า "ไม่พบตัวบท"

**พิสูจน์ว่าปลดล็อกแล้วได้ผลจริง** — ค้น "บ่อดักไขมัน 2565" แบบไม่จำกัดโดเมน
```
✓ official  lib3.dss.go.th/fulltext/fcm/P435_THAI.pdf     ← ไม่เคยอยู่ในรายชื่อ
✓ primary   env.anamai.moph.go.th/th/law/210690           ← มีตัวบทจริง
✓ primary   laws.anamai.moph.go.th/th/doh-annuance/210710 ← มีตัวบทจริง
✗ none      fdasamutsongkhram.org/…                       ← .org ไม่ใช่ .or.th
```
`lib3.dss.go.th` (กรมวิทยาศาสตร์บริการ) เป็นตัวอย่างตรงประเด็น — เดิมโมเดลไม่มีทางเห็นเลย

`SOURCE_URL_RULES` เขียนลำดับความน่าเชื่อถือ 4 ชั้นไว้ใน prompt และย้ำว่า
**เจอเนื้อหาจากบล็อกแล้วมั่นใจว่าถูก ก็ห้ามเอามาตอบ** ให้ใช้เป็นเบาะแสหาชื่อฉบับแทน
prompt กับด่านตรวจอยู่ไฟล์เดียวกันโดยตั้งใจ — แยกกันแล้วมันจะเพี้ยนจากกันโดยไม่มีใครรู้

---

> **ผลตรวจสอบทั้งชุด P20–P26 (ยิงจริง 2026-08-22):** [`docs/gazette-index-verification.md`](docs/gazette-index-verification.md)
> — กราฟการเรียกของ API · ความถูกต้องของข้อมูลในดัชนี · บั๊กที่จับได้ก่อนขึ้น · สิ่งที่ยังพิสูจน์ไม่ได้

> ทุก Skill ทำงานบน Vercel เท่านั้น (ต้อง deploy + ตั้ง `ANTHROPIC_API_KEY`)
> รัน `npm run dev` เฉยๆ ปุ่มจะเรียก API ไม่ได้
